#ifndef _SORTHELPER_H_
#define _SORTHELPER_H_

#include <stdlib.h>

/*
 * This function swaps two numbers
 *
 * param[in] px - pointer that points to an integer
 * param[in] py - pointer that points to the other integer
 *
 * returns - nothing
 */
void swap(int* px, int* py);

#endif //_SORTHELPER_H_
